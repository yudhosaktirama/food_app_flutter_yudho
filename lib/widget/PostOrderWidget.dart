import 'package:flutter/material.dart';

class PostOrderWidget extends StatelessWidget {
  const PostOrderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Ini Post ORder");
  }
}